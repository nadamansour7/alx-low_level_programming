#include"main.h"
/**
 * main - function that computes the absolute
 * value of integer
 *
 * @n: takes the integer type input for function
 *
 * Return: Always 0.
*/
int _abs(int)
{
    if (n < 0)
	    n = (-1) * n;
    return (n);
}
